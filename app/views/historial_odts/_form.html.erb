<%= form_for(@historial_odt) do |f| %>
  <% if @historial_odt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historial_odt.errors.count, "error") %> prohibited this historial_odt from being saved:</h2>

      <ul>
      <% @historial_odt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Comentarios %><br />
    <%= f.text_field :Comentarios %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
