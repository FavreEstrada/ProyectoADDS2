<%= form_for(@historial_oct) do |f| %>
  <% if @historial_oct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historial_oct.errors.count, "error") %> prohibited this historial_oct from being saved:</h2>

      <ul>
      <% @historial_oct.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
