<h1>Listing cliente_juridicos</h1>

<table>
  <tr>
    <th>Nombreempresa</th>
    <th>Cargo</th>
    <th>Responsable</th>
    <th>Fax</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cliente_juridicos.each do |cliente_juridico| %>
  <tr>
    <td><%= cliente_juridico.NombreEmpresa %></td>
    <td><%= cliente_juridico.Cargo %></td>
    <td><%= cliente_juridico.Responsable %></td>
    <td><%= cliente_juridico.fax %></td>
    <td><%= link_to 'Show', cliente_juridico %></td>
    <td><%= link_to 'Edit', edit_cliente_juridico_path(cliente_juridico) %></td>
    <td><%= link_to 'Destroy', cliente_juridico, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cliente juridico', new_cliente_juridico_path %>
